# list_ports.ys
read_verilog -sv *.v

# Roda hierarquia sem síntese (preserva nomes originais)
hierarchy -auto-top

# Lista todos os módulos com suas portas
tee -o relatorio.txt stat

# Dump JSON com toda a estrutura
write_json estrutura.json